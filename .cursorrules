# 智城云 · Cursor 开发规范

> 配置体系职责边界见：`AI-CONFIG-BOUNDARIES.md`
> 本文件定位：Cursor 全局高优先级规则（开发约束 + 关键规范摘要）

## 项目基本信息（精简版）

### 枚举值定义（最重要）
```ts
// events.status
pending | doing | escalated | done | closed

// events.scene
ioc | community | emergency | traffic | service

// events.level
high | mid | low
```

### 禁止行为（从 base 规则保留）
- 有真实后端接口时直接调用，禁止写 mock 代替真实链路。
- 新接口必须先后端（NestJS）后前端；禁止跳过后端直接写页面逻辑。
- 禁止降级处理（hardcode KPI、状态、流程）来“演示可跑”。
- 禁止为未开发后端接口提前创建 msw handler。
- 禁止无关改动：只改任务相关文件，不顺手重构。
- TypeORM CLI 禁止 `npx typeorm ...`，必须使用项目脚本。

## 架构约定（关键）

### Socket 事件名约定
- 后端统一推送 `event_updated`，子类型由 payload.`type` 区分。
- `new_message` 仅用于消息中心未读刷新。
- 推送事件后必须触发全链路联动刷新：至少刷新 `events` 与 `kpi`。

### 多租户隔离规则
- 所有业务查询必须带 `tenantId` 过滤条件。
- 非 `super_admin` 强制使用 JWT 中 `tenantId`，不信任前端传入。
- `super_admin` 可通过 `?tenantId=xxx` 切换视角。

### sceneConfig 使用规范
- 场景差异统一由 `sceneConfig.ts` 驱动，组件内禁止写死场景分支。
- 地图实例只允许初始化一次，场景切换仅更新覆盖物，不得重建底图。

## 状态机约定（从 02-state-machine 抽取）

### 合法流转
```text
pending -> doing
doing -> escalated
doing -> done
doing -> closed
escalated -> doing
escalated -> closed
pending -> closed

禁止：done/closed -> 任意状态
禁止：跨状态跳转（如 pending -> done）
```

### 接口校验顺序（最重要）
1. 资源存在性（404）
2. 状态合法性（400）
3. 权限校验（403）
4. 执行业务逻辑

关键前置检查：
- `dispatch`：仅 `pending` 可派单。
- `claim`：仅 `pending` 且 `assigneeId` 为空；并发冲突返回 `409`。
- `start`：仅 `doing`，且必须本人工单。
- `escalate`：仅 `doing` 且 `startedAt` 非空，且必须本人工单。
- `reassign`：仅 `doing/escalated`，改派后清理 `startedAt`/升级信息。
- `complete`：`pending` 的 `400` 优先于 assignee 的 `403`。

## 接口规范（从 03-api-contract 抽取）

### HTTP 状态码语义
- `200`：查询成功
- `201`：创建/派单/完结成功
- `400`：业务规则拒绝
- `401`：未登录或 token 失效
- `403`：无权限
- `404`：资源不存在
- `409`：并发冲突

### 统一响应格式
```json
{ "code": 0, "data": {}, "message": "ok" }
```
- Controller 只返回业务数据，禁止手动包装 `{ code, data, message }`。

## 测试规范（从 04-testing 抽取）

### 提交前 Checklist
- [ ] 改动对应模块测试通过（状态机/消息/KPI/租户隔离）。
- [ ] 多租户隔离未破坏。
- [ ] 无硬编码 tenantId 或绕过权限逻辑。
- [ ] 无 `console.log` 调试残留（允许 `console.error`）。

## 原有高价值约束保留

### 样式与路径规范
- 样式统一使用 `.scss` + CSS Modules（禁止 `.css`）。
- 使用全局 CSS 变量（`var(--xxx)`），禁止随意新增魔法颜色。
- 模块引入统一 `@/` 别名，禁止 `../` 跨层相对路径。

### 前端请求与交互规范
- `baseURL` 统一 `/api`，通过 Vite proxy 转发。
- 请求层已解包 `data`，store 禁止写 `res.data.data`。
- `apps/miniprogram` 所有接口按钮必须“UI 禁用 + 函数内 guard”双保险，防重复提交。

### 命令执行约束
- 不自动执行安装依赖、启动服务、构建、迁移等耗时命令。
- migration 命令只列出，需用户确认后手动执行。

